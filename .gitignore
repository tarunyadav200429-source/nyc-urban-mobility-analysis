# Python virtual environment (large, machine-specific — never commit)
.venv/
venv/
__pycache__/
*.pyc

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
*~

# Large raw datasets & the working database — reproducible via the scripts, not committed
data/raw/*.parquet
data/processed/*.db

# Screenshots / Power BI temp files
Screenshot *.png
*.pbix.tmp
.~lock.*
